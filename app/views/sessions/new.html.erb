
<h1 class="ui center aligned large header"> Witamy w Pogadajmy - aplikacji do pogaduszek</h1>
<h4 class="ui center aligned medium icon header ">
    <i class="circular blue power off icon"></i>
</h4>
<h1 class="ui center aligned medium header">
    Zaloguj się, aby kontynuować
</h1>


<div class="ui placeholder segment">
  <div class="ui two column very relaxed stackable grid">
    <div class="column">
      <%= form_for(:session, html: {class: "ui form", role: "form"}, url: login_path) do |f| %>
        <div class="field">
          <%= f.label :username, "Użytkownik" %>
          <div class="ui left icon input">
            <%= f.text_field :username, placeholder: "Użytkownik" %>
            <i class="user icon"></i>
          </div>
        </div>
        <div class="field">
          <%= f.label :password, "Hasło" %>
          <div class="ui left icon input">
            <%= f.password_field :password, placeholder: "Hasło" %>
            <i class="lock icon"></i>
          </div>
        </div>
        <%= f.button "Zaloguj", class: "ui blue submit button" %>
      <% end %>
    </div>
    <div class="middle aligned column">
      <div class="ui big blue button">
        <i class="signup icon"></i>
        Zapisz się
      </div>
    </div>
  </div>
  <div class="ui vertical divider">
    Lub
  </div>
</div>